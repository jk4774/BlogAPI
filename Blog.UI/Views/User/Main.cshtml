@model Blog.API.Models.FullUser;
@{
    ViewData["Title"] = "User";
}
<div id="main">
    
    <div class="col-sm-5 text-center h3">
        @Model.User.Name
    </div>

    <div class="col-sm-2 text-center h3">
        <form action="/article/create" method="get">
            <input type="submit" class="btn btn-outline-primary btn-lg" value="Create Article">
        </form>
    </div>

    <div class="col-sm-2 text-center h3">
        <form action="/user/update" method="get">
            <input type="submit" class="btn btn-outline-primary btn-lg" value="Change Password">
        </form>
    </div>

    <div class="col-sm-2 text-center h3">
        <input type="submit" class="btn btn-outline-primary btn-lg" id="deleteUser" value="Delete User">
    </div>

    <div class="col-sm-1 text-center h3">
        <form action="/user/logout" method="post">
            <input type="submit" class="btn btn-outline-primary btn-lg" value="Logout">
        </form>
    </div>

    <div class="col-sm-12">
        @if (@Model.Articles != null)
        {                
            @foreach (var Article in @Model.Articles.OrderBy(x => x.Key.Date))
            {
                <div class="col-sm-12 h5 border border-primary rounded">
                    @if (@Article.Key.UserId == @Model.User.Id)
                    {
                        <p class="col-sm-6 text-center h4">@Article.Key.Title</p>
                        <div class="col-sm-3 text-center h5">
                            <form action="/article/update/@Article.Key.Id" method="get">
                                <input type="submit" class="btn btn-outline-primary btn-lg" value="Update Article">
                            </form>
                        </div>    
                        <div class="col-sm-3 text-center h5">
                            <input type="submit" class="btn btn-outline-primary btn-lg" id="deleteArticle" value="Delete Article">
                            <input type="hidden" id="article-id" value="@Article.Key.Id">
                        </div>
                    }
                    else
                    {
                        <p class="col-sm-12 text-center h4">@Article.Key.Title</p>
                    }
                    
                    <p class="col-sm-12 text-left h5">@Article.Key.Content</p>
                    <div class="col-sm-12 h5">
                        <span class="text-left">Author: @Article.Key.UserName</span>
                        <span class="text-right">Last edit: @Article.Key.Date</span>
                    </div>
                </div>
                @if (@Article.Value != null)
                {
                    @foreach (var Comment in @Article.Value)
                    {

                    }
                }
            }
        }
    </div>
</div>
<script>
    document.getElementById('deleteUser').addEventListener('click', () => {
        var xhr = new XMLHttpRequest(); 
        xhr.open('DELETE', '/user/' + @Model.User.Id);
        xhr.onload = () => {
            if (xhr.status != '204') 
                return alert('Something went wrong, cannot delete the user.');
            alert("The user has been deleted correctly.");
            window.location.href = '/';
        };
        xhr.send();
    });
    document.getElementById('deleteArticle').addEventListener('click', () => {
        let id = document.getElementById('').value;
        var xhr = new XMLHttpRequest();
        xhr.open('DELETE', '/article/' + );
        xhr.onload = () => {
            if (xhr.status != '204') 
                return alert('Something went wrong, cannot delete this article.');
            alert('This article and all comments had been deleted correctly.');
            window.location.href = '/';
        };
        xhr.send();
    });
</script>
